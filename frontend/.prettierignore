# Generated by cmd/genenums from the Go typed-const enum blocks. Do not format —
# the file is regenerated on `npm install` / `npm run generate`, and its content
# drift is guarded by the `cmd/genenums -compare` CI gate (formatting-agnostic).
src/generated/
