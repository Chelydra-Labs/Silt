<!-- Next release notes go here.

The create-pr skill (see `~/.config/opencode/skill/create-pr/SKILL.md`) overwrites
this file when opening a PR. The release workflow at
`.github/workflows/release.yml` reads it, posts it to the GitHub Release, and
resets it to this stub via a `[skip ci]` commit.

If this file still has real content after a release has shipped, the release
workflow failed to reset it — investigate before the next release.
-->
